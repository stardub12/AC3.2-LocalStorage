# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AC3.2-LocalStorage' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AC3.2-LocalStorage
  pod 'SnapKit', '~> 3.1.2' # this is the same as saying "up to version 3.2, not including 3.2 and higher"

end
